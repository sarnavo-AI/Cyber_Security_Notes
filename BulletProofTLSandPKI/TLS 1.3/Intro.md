When we peer inside the protocol, however, we can see that it consists of two layers. The **record protocol is the outer shell in TLS**, designed to carry, encrypt, and protect messages containing opaque data. 

