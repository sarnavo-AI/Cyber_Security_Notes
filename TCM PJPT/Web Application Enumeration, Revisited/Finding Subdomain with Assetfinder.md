with golang,

![[Pasted image 20250711174334.png]]
writing shell script to do the work for us, 

`assetfinder tesla.com` will help us find subdomain which are sued by tesla.com too, so to filter it out we can use a `grep` command (recommended) or `assetfinder --sub-only tesla.com` to filter
